[buildout]
parts = python django
eggs =
  BeautifulSoup
  django-debug-toolbar
  django-extensions
  django-registration
  django-staticfiles
  gunicorn
  Markdown
  south
unzip = true

[python]
recipe = zc.recipe.egg
interpreter = python
eggs = ${buildout:eggs}

[django]
recipe = djangorecipe
version = 1.2.1
project = website
projectegg = website
settings = settings
test = adventure profile
eggs = ${buildout:eggs}
extra-paths =
  src
